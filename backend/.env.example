NODE_ENV=development
PORT=4000
WEB_URL=http://localhost:3000

# Supabase — get from Supabase project settings > API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Clerk — get from Clerk dashboard > API Keys
CLERK_SECRET_KEY=sk_test_...

# AWS S3 — IAM user with s3:PutObject, s3:GetObject, s3:DeleteObject on the bucket
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=contralyn-contracts

# Anthropic — get from console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...
AI_MODEL=claude-sonnet-4-6

# SMTP - admin password-reset emails (optional; reset stays disabled when unset)
# Gmail: host smtp.gmail.com, port 587, user = full address, pass = app password
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
