{
  "version": 2,
  "builds": [
    {
      "src": "src/app.ts",
      "use": "@vercel/node",
      "config": { "maxLambdaSize": "50mb", "maxDuration": 120 }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/app.ts" }
  ]
}
