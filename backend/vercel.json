{
  "version": 2,
  "builds": [
    {
      "src": "src/app.ts",
      "use": "@vercel/node",
      "config": { "maxLambdaSize": "50mb" }
    }
  ],
  "functions": {
    "src/**/*.ts": {
      "maxDuration": 60
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "src/app.ts" }
  ]
}
